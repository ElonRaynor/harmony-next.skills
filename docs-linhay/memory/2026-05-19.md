# 2026-05-19

## issue 9 guide 离线化第十一轮

- 继续处理 HarmonyOS 在线 guide 外链离线化，将全部 `4` 次残留 slug 收录到 `references/JsEtsAPIReference/guides/`，并接入 `harmony-next/scripts/reference_compat.py` 的 `TARGETED_GUIDE_MAPPING`。
- 新增离线页包括：`用户文件URI介绍.md`、`耗时任务并发场景简介.md`、`智能填充服务.md`、`申请推送场景化消息权益.md`、`开放权限（用户授权）.md`、`云侧服务准备.md`、`基于服务账号生成鉴权令牌（Device Security）.md`、`非对称密钥生成和转换规格.md`、`wrapBuilder：封装全局@Builder.md`、`支持键盘输入事件.md`、`Environment：设备环境查询.md`、`UIAbility备份恢复.md`。
- 执行 `reference_compat.py generate` 后实际重写 23 个 guide-link 文件；`check`、`audit`、`python3 -m unittest discover -s harmony-next/tests -p 'test_*.py' -v` 均通过。
- 当前仓库内 HarmonyOS 在线 guide slug 最高残留频次已降到 `3` 次，`4` 次及以上高频头部已清空。

## skill 文档 AI 可用性核对

- 核对最近提交的 `harmony-next/SKILL.md` 与 DevEco 私有接口参考页，确认问题主要是新增参考页偏“调研报告/过程记录”口吻，不利于 agent 直接执行。
- 已将 `SKILL.md` 调整为 agent guide，并将 DevEco Emulator / DevEco Studio IDE 两个私有接口参考页重写为 playbook，突出触发场景、默认允许/需确认/禁止动作、超时、脱敏和输出格式。
- 后续新增 skill 文档应优先沉淀 agent 触发条件、读取顺序、动作边界和输出规则，避免把内部验证过程直接作为 skill 主体。

## subagent 体验后的 skill 文档收紧

- 使用三个 subagent 分别体验 `harmony-next/SKILL.md`、DevEco Emulator playbook 和 DevEco Studio IDE playbook，结论是整体可用，但入口路径、失败分流、确认话术和隐私边界还需加强。
- 已补充 `references/...` 路径相对 `harmony-next/` 的说明、普通 API 查询与 DevEco playbook 的排他路由、Emulator 最小只读探测/失败分流/确认模板，以及 IDE `strings/rg` 敏感输出、用户侧目录和 MCP/model provider 配置读取边界。

## skill 版本与安装信息

- 将 `harmony-next/SKILL.md` 中原本仅隐藏注释存在的版本信息改为显式 `Version` 章节，当前本地技能包版本为 `v1.3.5`。
- 明确 `references/` 是 HarmonyOS API 12-23 的离线快照，不等同于在线实时文档；遇到 latest/current、新 API 或在线文档一致性问题时，需要对比 GitHub Releases、nightly 包和华为在线文档。
- 在 `SKILL.md` 中补充 Gemini CLI、Claude.ai / Claude Code、Codex 的安装或更新入口，并新增 `test_skill_metadata.py` 防止后续版本/安装信息缺失。
- 进一步按渐进式披露优化：版本进入 frontmatter `metadata.version`，正文章节收敛为 `Version`，仅保留 Agent 决策所需的最小安装入口。
- 新增 `sync_release_version.py` 并接入 GitHub Actions：tag 发布或手动指定版本发布时，CI 会在打包前同步 `SKILL.md` 与中英文 README 的版本信息，并先跑 unittest；nightly 不改稳定版本。
