# 2026-05-28

## Issue 13: Emulator CLI trace preflight

- 处理 GitHub issue #13：`DevEco Emulator CLI path should guard against login-gated startup dialog`。
- 明确 DevEco Emulator 登录/设备管理弹窗“模拟器启动失败 / 请在DevEco Studio中登录华为账号，并从设备管理中启动模拟器”可由不完整 CLI 启动路径触发；优先归类为缺少 `-t <trace-name>` / trace pipe helper，而不是直接要求用户登录。
- `harmony-next/scripts/hvd_manager.py` 新增 `launch-preflight`：只验证 HVD、Emulator、SDK root、trace name 和 helper readiness；helper 不可用时返回 `blocked`，满足时只输出带 `-t` 的命令计划，不直接启动 Emulator。
- 同步更新 `harmony-next/SKILL.md`、DevEco 模拟器 playbook、`docs-linhay/dev/2026-05-18-deveco-emulator-cli.md` 和 issue 专项研发文档。
- 验证：`python3 -m unittest harmony-next/tests/test_hvd_manager.py` 通过 12 个测试；`python3 -m unittest discover -s harmony-next/tests` 通过 36 个测试。
